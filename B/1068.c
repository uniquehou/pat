#include <stdio.h>
#include <string.h>
int main() {
	int row, col, TOL;
	scanf("%d %d %d", &col, &row, &TOL);
	int color[1000][1000];
	for(int i=0; i<row; i++)
		for(int j=0; j<col; j++)
			scanf("%d", &color[i][j]);

	for(int i=0; i<row; i++)
		for(int j=0; j<col; j++) {
			
		}
}